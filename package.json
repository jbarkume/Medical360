{
    "scripts": {
        "build": "cd client && npm install && npm run build",
        "start": "cd server && npm install && node seed.js && npm start",
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "engines": {
        "node": "21.x",
        "npm": "10.x"
    },
    "dependencies": {
        "@vitejs/plugin-react": "^4.2.1",
        "autoprefixer": "^10.4.17",
        "jest": "^29.7.0",
        "js-cookie": "^3.0.5",
        "postcss": "^8.4.33",
        "supertest": "^6.3.4",
        "tailwindcss": "^3.4.1",
        "vite": "^5.2.6"
    }
}
